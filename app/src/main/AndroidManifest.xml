<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        tools:targetApi="31">
        <activity
            android:name=".delivery_page"
            android:exported="true"
            android:theme="@style/Theme.DemoZomato" />
        <activity
            android:name=".person_about"
            android:exported="true"
            android:theme="@style/MyDetailTheme.DemoZomato.." />
        <activity
            android:name=".billing_View"
            android:exported="true"
            android:theme="@style/Theme.DemoZomato" />
        <activity
            android:name=".description_view"
            android:exported="true"
            android:theme="@style/Theme.DemoZomato" />
        <activity
            android:name=".hotels_list_page"
            android:exported="true"
            android:theme="@style/Theme.DemoZomato" />
        <activity
            android:name=".SplashScreen"
            android:exported="true"
            android:theme="@style/MyTheme.DemoZomato.">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.DemoZomato" />
    </application>

</manifest>